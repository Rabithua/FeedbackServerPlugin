#!/usr/bin/env bun

import {
  ensureRuntimeDependencies,
  relaunchCurrentProcess,
} from '../scripts/ensure-runtime-dependencies.ts';

if (ensureRuntimeDependencies()) {
  process.exit(await relaunchCurrentProcess());
}
await import('../dist/server.mjs');
