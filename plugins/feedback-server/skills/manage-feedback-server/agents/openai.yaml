interface:
  display_name: "Manage FeedbackServer"
  short_description: "Route broad FeedbackServer work to a focused workflow"
  default_prompt: "Use $manage-feedback-server to route this broad FeedbackServer request to the right focused workflow."
dependencies:
  tools:
    - type: "mcp"
      value: "feedback-server"
      description: "FeedbackServer administrator tools"
      transport: "http"
policy:
  allow_implicit_invocation: true
